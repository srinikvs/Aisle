# Copy to .env.local for Vite. Leave blank to use on-device demo accounts.
# Production (Sarukulu / Playadda) must set both so households sync in the cloud.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
